import styled from 'styled-components';

export const StylesLink = styled.div`
    h2 {
        font-size: 18px;
        color: var(--primary);
        font-weight: 700;
    }

    p {
        color: var(--gray);
        font-size: 16px;
    }
`;
